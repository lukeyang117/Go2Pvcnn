# Codex Repository Entry

Before any analysis, code reading, code changes, script execution, training, evaluation, or completion claims in this repository, read and follow:

- `notes/index.md`
- `.codex`

This file is a hard repository constraint for `/home/lhy/testPvcnnWithIsaacsim`.

Hard requirements:

- Every new conversation in this repository must begin by checking `notes/index.md` first.
- The notes knowledge index is part of the working context, not optional background material.
- All note links must use repository-relative paths so the vault works both on the server and through the local `Z:` SSHFS mount in Obsidian graph view.
- `raw/` and `onlyReference/` are reference areas by default, not the active implementation target, unless the user explicitly says otherwise.
- Do not claim a fix for training, simulation, observation wiring, curriculum logic, LiDAR, or PVCNN integration without stating what was verified.
- For tasks involving `Go2Pvcnn/extension/planner`, trajectory imitation rewards, or raw-to-extension planner alignment, read:
  - `notes/human/human-08-extension-planner-reading-guide.md`
  - `notes/human/human-09-extension-planner-mapping.md`
  - and the relevant `raw/kinematic_footsteps/notes` documents before editing planner core logic.

Operational boundaries:

- Active implementation work should target the root project and `Go2Pvcnn/` unless the user redirects scope.
- Avoid editing `third_party/` unless the task explicitly requires vendored code changes.
- Prefer adding project knowledge to `notes/` or `docs/` rather than scattering scratch files at the repository root.

If this repository-level guidance conflicts with a direct user instruction in the current conversation, follow the user and call out the override explicitly.
